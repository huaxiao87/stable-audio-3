# Stable Audio 3 在 RTX 5090 上的完整指南

这份文档汇总了从「Python 包管理选型」到「在 RTX 5090 上跑通 Stable Audio 3 medium」的全过程与所有踩过的坑。
项目目录:`/mnt/intel-nvme/stable-audio-lab`(即官方 `stable-audio-3` 仓库 + 针对 5090 的改动)。

---

## 1. 包管理:为什么用 uv

AI 训练长期来看,**`uv` 是目前的最佳实践**,可替代 pip + venv + pyenv。

| 工具 | 定位 | 评价 |
|------|------|------|
| **pip** | 只装包,不管环境/Python 版本 | 太原始,单用不够 |
| **conda/Anaconda** | 包 + 环境 + 非 Python 二进制 | 慢、臃肿,商业用途有 license 风险;GPU 场景已被 pip wheel 取代 |
| **uv** | Rust 写的超快 pip+venv+python 版本管理 | ✅ 当前新标准,快 10–100 倍,lockfile 可复现 |

核心理由:
- **快**:依赖求解和安装比 pip/conda 快一个数量级。
- **可复现**:`pyproject.toml`(声明)+ `uv.lock`(锁定精确版本),换机器 `uv sync` 一条命令重建。
- **GPU 不再需要 conda**:PyTorch 的 pip wheel 自带打包好的 CUDA 运行时,主机只要有合适的 NVIDIA 驱动即可,不必装系统级 CUDA toolkit(编译自定义算子除外,见第 5 节)。

常用命令:

```bash
uv add <包>          # 增加依赖 (自动写 pyproject + uv.lock)
uv remove <包>       # 删除依赖
uv sync              # 按 lockfile 重建环境
uv sync --inexact    # 重建但不删除「不在 lockfile 里」的包 (如 flash-attn, 重要!)
uv run <命令>        # 在环境里执行, 不用手动 activate
```

> uv 装在 `~/.local/bin`。若 `uv: command not found`,把这行加进 `~/.bashrc`:
> `source $HOME/.local/bin/env`

---

## 2. 关键事实:Stable Audio 3 ≠ Stable Audio Open(1.0)

- **Stable Audio 3**(2026‑05 发布)用的是**全新仓库 `Stability-AI/stable-audio-3` + 库 `stable_audio_3`**。
- 旧的 `stable-audio-tools`(1.0 / Open)是另一套,API 不同,**别混用**。
- 模型族:

| 模型 | ID | 硬件 | 参数 | 最长 | 开源 |
|------|----|------|------|------|------|
| Small‑Music | `small-music` | CPU | 433M | 120s | ✅ 无需 flash-attn |
| Small‑SFX | `small-sfx` | CPU | 433M | 120s | ✅ 无需 flash-attn |
| **Medium** | `medium` | **GPU(CUDA)** | 1.4B | 380s | ✅ **需 flash-attn**(本机主力) |
| Large | — | 仅 API | 2.7B | 380s | ❌ 不开源 |

---

## 3. 本机环境(已全部实测验证 ✅)

```
项目          : /mnt/intel-nvme/stable-audio-lab
GPU           : NVIDIA RTX 5090, capability (12,0) = sm_120
驱动 (UMD)    : CUDA 13.3 (向下兼容 cu128 wheel)
CUDA Toolkit  : 12.8 (系统安装, 仅用于编译 flash-attn)
Python        : 3.11
包管理        : uv
torch         : 2.7.1+cu128
flash-attn    : 2.8.3 (为 sm_120 源码编译, 已实跑验证)
端到端        : medium 文本生成音乐 → 21M 立体声 44.1kHz wav ✅
```

### 为什么是这些版本

- **torch 2.7.1 + cu128**:RTX 5090 是 Blackwell **sm_120**,必须 CUDA 12.8+ 的 wheel(cu126 及以下无 sm_120 内核,会报 `no kernel image` 或回退 CPU)。而 `torch==2.7.1` 又是 SA3 钉死的版本,两者正好都满足。
- **cu128 而非默认 cu126**:SA3 的 `pyproject.toml` 默认装 cu126,这是**唯一针对 5090 改的地方**——把 PyTorch 索引改成了 cu128。
- **Python 3.11**:SA3 要求 `>=3.10`,3.11 兼容性与 wheel 覆盖都好。

---

## 4. 从零重建环境

```bash
cd /mnt/intel-nvme/stable-audio-lab

uv sync                              # 基础 (Python API + CLI)
uv sync --extra ui                   # + Gradio 界面
uv sync --extra lora                 # + LoRA 训练
uv sync --extra ui --extra lora      # 全部
```

> ⚠️ 上面几条都会因为 flash-attn 不在 lockfile 里而**把它删掉**!
> 装好 flash-attn 后,再次 sync 一律用 **`uv sync --inexact`**。

---

## 5. flash-attn(medium 必需)是怎么搞定的

`medium` 依赖 Flash Attention 2(`from flash_attn import flash_attn_func`),否则输出是噪音。
难点:**没有** `cu128 + torch2.7 + cp311 + sm_120` 的现成 wheel,只能从源码为 sm_120 编译。

踩过的坑:
- pip 的 `nvidia-cuda-nvcc-cu12` 在 Linux 上**只带 `ptxas`,没有 `nvcc` 前端**,所以无法仅靠 pip 在 venv 内编译 → 必须有真正的系统 CUDA Toolkit。

最终做法(已成功):

```bash
# 1) 系统装 CUDA Toolkit 12.8 (Ubuntu 24.04, 需 sudo, 一次性)
wget https://developer.download.nvidia.com/compute/cuda/repos/ubuntu2404/x86_64/cuda-keyring_1.1-1_all.deb
sudo dpkg -i cuda-keyring_1.1-1_all.deb
sudo apt-get update && sudo apt-get install -y cuda-toolkit-12-8

# 2) 为 sm_120 编译 flash-attn (本机 384 核, 很快)
export CUDA_HOME=/usr/local/cuda-12.8
export PATH="$CUDA_HOME/bin:$PATH"
cd /mnt/intel-nvme/stable-audio-lab
uv pip install ninja packaging psutil
TORCH_CUDA_ARCH_LIST="12.0" MAX_JOBS=24 \
  uv pip install flash-attn==2.8.3 --no-build-isolation

# 3) 验证 (能在 5090 上跑 flash_attn_func 即成功)
uv run python -c "from flash_attn import flash_attn_func; import flash_attn; print(flash_attn.__version__)"
```

> `TORCH_CUDA_ARCH_LIST="12.0"` 就是 sm_120(RTX 5090)。

---

## 6. 跑 medium:完整步骤

### 6.1 拿访问权限(门控模型,一次性)

1. 打开 https://huggingface.co/stabilityai/stable-audio-3-medium → 点 **Agree and access**。
2. https://huggingface.co/settings/tokens → 建一个 **Read** token。

### 6.2 登录(命令是 `hf`,不是已废弃的 `huggingface-cli`)

```bash
cd /mnt/intel-nvme/stable-audio-lab
uv run hf auth login          # 粘贴 token
# 或: export HF_TOKEN=hf_xxxxx
```

### 6.3 生成(首次自动下载权重约 9G,之后缓存)

```bash
uv run stable-audio --model medium \
  -p "A triumphant UK bass tech-house tune, pumping four-to-the-floor kick, 808 bass, euphoric gospel house piano. 128 BPM" \
  --duration 60 --steps 8 --seed 42 \
  -o outputs/techhouse.wav
```

模型缓存后,8 步采样不到 1 秒(对抗后训练,极快)。

### 6.4 用现成示例脚本(推荐)

```bash
bash examples.sh login      # 登录
bash examples.sh basic      # 文本生成音乐 (60s)
bash examples.sh long       # 长曲 (240s)
bash examples.sh batch      # 一次出多条
bash examples.sh sfx        # 音效/短采样
bash examples.sh a2a        # 音频转音频 (改风格)
bash examples.sh continue   # 续写/延长
bash examples.sh ui         # Gradio 网页界面
```

### 6.5 Python 方式

```python
from stable_audio_3 import StableAudioModel

model = StableAudioModel.from_pretrained("medium")
audio = model.generate(prompt="House music, festival vibe, 124 BPM", duration=120)
# audio 是张量, 用 torchaudio.save 保存
```

### CLI 关键参数

| 参数 | 默认 | 说明 |
|------|------|------|
| `--model` | `medium` | `medium` / `small-music` / `small-sfx` … |
| `--steps` | `8` | medium 后训练版少步即可 |
| `--cfg-scale` | `1.0` | base 模型用 7.0 |
| `--duration` | `120` | 秒;medium 上限 380 |
| `--seed` | `-1` | -1 随机;固定值可复现 |
| `--init-audio` / `--init-noise-level` | — | 音频转音频 |
| `--inpaint-audio` / `--inpaint-start/end` | — | 续写 / 局部重绘 |

---

## 7. 常见坑速查

| 现象 | 原因 | 解决 |
|------|------|------|
| `LibsndfileError: System error` 保存失败 | 输出目录(如 `outputs/`)不存在 | `mkdir -p outputs`;`examples.sh` 已自带 |
| `uv: command not found` | uv 在 `~/.local/bin` 没进 PATH | `source $HOME/.local/bin/env`,或加进 `~/.bashrc` |
| `huggingface-cli` 找不到 | 新版 hub 改名为 `hf` | 用 `hf auth login` |
| `uv sync` 后 flash-attn 没了 | 它不在 lockfile 里 | 改用 `uv sync --inexact` |
| `no kernel image is available` | 用了非 cu128 的 torch | 确认 `torch.__version__` 带 `+cu128` |
| medium 输出是噪音 | flash-attn 没装好 | 见第 5 节验证 flash_attn_func |

---

## 8. 提示词建议

写清楚 **流派 / 乐器 / 情绪 / BPM**,越具体越好。仓库里 `docs/guides/prompting.md` 有大量官方示例提示词,以及音频转音频、续写/局部重绘的用法说明。
