# 个人实验配置

存放 LoRA 训练、数据路径等个人配置，例如：

- `lora_small_music.yaml` — 指向 `../../scripts/train_lora.py` 的超参
- `dataset_paths.env` — 本地数据目录（勿提交密钥）

完整 LoRA 流程见 [docs/workflows/lora.md](../../docs/workflows/lora.md)。
