# §3.2 Flow Matching

> 阅读日期：

## 核心概念

- `xt = (1-t)x₀ + tε`
- 速度目标 `v = ε − x₀`
- Minibatch OT
- inpaint loss 拆分

## 代码线索

- `training/diffusion.py` → `training_step()`
- `ot_coupling`, `random_inpaint_mask()`, `targets = noise - diffusion_input`

## 待澄清

-
